import type { Theme } from "./modern-minimal.ts";

/** warm-professional — muted warm neutrals, suited to trust-driven services. */
export const warmProfessional: Theme = {
  id: "warm-professional",
  cssVariables: {
    background: "#fdfaf6",
    foreground: "#2e2a24",
    accent: "#b5651d",
    muted: "#7a6f5f",
  },
};
